[package]
name = "emsl-core"
description = "Fidelity-neutral market-simulation primitives: units, orders and fills, the netted position, the account, the cost model, and the resting-order book."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dev-dependencies]
proptest = { workspace = true }
